@model IEnumerable<test_app.Models.PatProcedure>

<h1>PatProcedures</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>Id</th>
        <th>PatId</th>
        <th>ProcId</th>
        <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var patProcedure in Model)
    {
        <tr>
            <td>@patProcedure.Id</td>
            <td>@patProcedure.PatId</td>
            <td>@patProcedure.Procid</td>
            <td>
                <a asp-action="Edit" asp-route-id="@patProcedure.Id" class="btn btn-primary">Edit</a>
                <form asp-action="Delete" method="post" style="display: inline;">
                    <input type="hidden" name="id" value="@patProcedure.Id"/>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>
            </td>
        </tr>
    }
    </tbody>
</table>
